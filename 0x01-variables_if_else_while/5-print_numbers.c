#include <stdio.h>
/**
 * main - digit
 *
 * Description: program that prints all single digit numbers of base
 * Return: 0
 */
int main(void)
{
	int i;

	for (i == 0; i <= 10; i++)
{
	printf("%i\n", i);
	return (0);
}
