#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - main block
 * return: 0
 */
int main(void)
{
		int n;

		srand(time(0));

				n = rand() - RAND_MAX / 2;
		int randomNumber;

		for (int n = 0; n < 0; n > 0)
		if (n == 0)
		printf("is zero\n");
		{
		else if (n < 0)
		printf("is negative\n");
		{
		else if (n > 0)
		printf("is positive\n");
}
