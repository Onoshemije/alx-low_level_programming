#include <stdlib.h>
#include <time.h>
/**
 * main - assigns a random number to the variable n
 * return: 0
 */
int main(void)
{
		int n;



			srand(time(0));

				n = rand() - RAND_MAX / 2;
				while (n > 0)
				{
					printf("is positive\n");
				while (n == 0)
				{
					printf("is zero\n");
				while (a < 0)
				{
					printf("is negative\n");
				}
					return (0);

}
